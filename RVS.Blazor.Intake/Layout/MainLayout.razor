@inherits LayoutComponentBase
@implements IDisposable
@inject ThemeService ThemeService

<MudThemeProvider Theme="ThemeService.CurrentTheme" IsDarkMode="ThemeService.IsDarkMode" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="2" Color="Color.Primary">
        <MudIconButton Icon="@Icons.Material.Filled.RvHookup"
                       Color="Color.Inherit"
                       Href="/"
                       aria-label="Back to the homepage" />
        <MudText Typo="Typo.h6" Class="ml-2">
            RV Service Flow
        </MudText>
        <MudSpacer />
        <ThemeSwitcher />
    </MudAppBar>

    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.Medium" Class="py-4 px-4">
            <ErrorBoundary>
                <ChildContent>
                    @Body
                </ChildContent>
                <ErrorContent Context="ex">
                    <MudAlert Severity="Severity.Error" Class="my-4">@ex.Message</MudAlert>
                </ErrorContent>
            </ErrorBoundary>
        </MudContainer>
    </MudMainContent>

    <footer class="pa-3" style="background: var(--mud-palette-background-gray);">
        <MudText Typo="Typo.caption" Style="color: var(--mud-palette-text-secondary);">
            &copy; @DateTime.UtcNow.Year RV Service Flow
        </MudText>
    </footer>
</MudLayout>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

@code {
    protected override void OnInitialized()
    {
        ThemeService.OnThemeChanged += StateHasChanged;
    }

    public void Dispose()
    {
        ThemeService.OnThemeChanged -= StateHasChanged;
    }
}
