# Visit Types Update Summary

## ? Changes Completed

### 1. **Seed Data Updated** (`BF.Data.Cosmos.Seed\Program.cs`)

Added comprehensive visit types to support `PatientCheckInService.GetServiceTypeCodesForVisit()` helper methods:

#### Routine Vision Care
- `RoutineVision` - Routine Vision Exam
- `AnnualVision` - Annual Vision Exam (NEW)
- `ComprehensiveExam` - Comprehensive Eye Exam (NEW)

#### Medical Eye Conditions
- `Medical` - Medical Eye Visit
- `EmergencyEye` - Emergency Eye Care
- `UrgentEyeCare` - Urgent Eye Care (NEW)
- `Infection` - Eye Infection (NEW)
- `Injury` - Eye Injury (NEW)

#### Specific Medical Conditions
- `Glaucoma` - Glaucoma Exam (NEW)
- `DiabeticEyeExam` - Diabetic Eye Exam (NEW)
- `RetinalExam` - Retinal Examination (NEW)

#### Contact Lenses
- `ContactLensFitting` - Contact Lens Fitting
- `CLFitting` - CL Fitting (abbreviation) (NEW)

#### Eyewear Services
- `GlassesDispensing` - Glasses Dispensing (NEW)
- `GlassesDispense` - Glasses Dispense
- `FrameSelection` - Frame Selection (NEW)

#### Follow-up and Special
- `Recheck` - Recheck Visit
- `Pediatric` - Pediatric Vision Exam

---

## ?? Data Flow Verification

### API ? Client ? UI Binding Chain

```
???????????????????????????????????????????????????????????????
? 1. COSMOS DB (lookups container)                            ?
?    ?? LookupSet: "visit-types" (TenantId: "GLOBAL")       ?
???????????????????????????????????????????????????????????????
                            ?
                            ?
???????????????????????????????????????????????????????????????
? 2. API LAYER                                                 ?
?    ?? GET /api/lookups/visit-types                         ?
?    ?? LookupsController.GetLookupAsync()                   ?
?    ?? LookupService.GetLookupSetAsync()                    ?
?    ?? LookupRepository.GetGlobalAsync("visit-types")       ?
?    ?? Returns: LookupSetDto                                ?
???????????????????????????????????????????????????????????????
                            ?
                            ?
???????????????????????????????????????????????????????????????
? 3. BLAZOR CLIENT (LookupApiService)                         ?
?    ?? GetVisitTypesAsync()                                 ?
?    ?? Deserializes: LookupSetDto                           ?
?    ?? Extracts: lookupSet.Items ? List<LookupItemDto>     ?
???????????????????????????????????????????????????????????????
                            ?
                            ?
???????????????????????????????????????????????????????????????
? 4. CACHE LAYER (LookupCacheService)                         ?
?    ?? LoadAsync(practiceId)                                ?
?    ?? _visitTypes = await GetVisitTypesAsync()             ?
?    ?? Property: VisitTypes ? IReadOnlyList<LookupItemDto> ?
???????????????????????????????????????????????????????????????
                            ?
                            ?
???????????????????????????????????????????????????????????????
? 5. UI COMPONENT (EncounterDetailsSection.razor)             ?
?    ?? _visitTypes = LookupCache.VisitTypes                 ?
?    ?? FluentSelect binds to _visitTypes                    ?
?    ?? OptionText="@(v => v.Name)"                          ?
?    ?? OptionValue="@(v => v.Code)"                         ?
?    ?? SelectedOption="_selectedVisitType"                  ?
???????????????????????????????????????????????????????????????
                            ?
                            ?
???????????????????????????????????????????????????????????????
? 6. USER SELECTION                                            ?
?    ?? User selects visit type from dropdown                ?
?    ?? OnVisitTypeChanged() fires                           ?
?    ?? State.SetVisitType(visitType.Code)                   ?
?    ?? Code stored in encounter state                       ?
???????????????????????????????????????????????????????????????
```

---

## ?? Testing Steps

### 1. **Run Seed Process**
```bash
cd BF.Data.Cosmos.Seed
dotnet run
```

Expected output:
```
Seeding lookups...
? Done seeding.
```

### 2. **Verify in Cosmos DB**
- Container: `lookups`
- Partition Key: `GLOBAL`
- Document ID: `visit-types`
- Verify `Items` array contains 19 visit types

### 3. **Start API**
```bash
cd BF.API
dotnet run
```

Test endpoint:
```bash
curl https://localhost:7116/api/lookups/visit-types
```

Expected response:
```json
{
  "category": "VisitType",
  "name": "Visit Types",
  "items": [
    { "code": "RoutineVision", "name": "Routine Vision Exam", ... },
    { "code": "AnnualVision", "name": "Annual Vision Exam", ... },
    { "code": "ComprehensiveExam", "name": "Comprehensive Eye Exam", ... },
    ...
  ]
}
```

### 4. **Start Blazor WASM**
```bash
cd BF.BlazorWASM
dotnet run
```

### 5. **Verify in Browser**
1. Navigate to `/practices/{practiceId}/check-in`
2. Open browser DevTools ? Network tab
3. Verify API call: `GET /api/lookups/visit-types` returns 200 OK
4. Check Visit Type dropdown populates with all 19 visit types
5. Select a visit type and verify state updates

---

## ?? Visit Type ? Service Type Code Mapping

The updated visit types now support `PatientCheckInService.GetServiceTypeCodesForVisit()`:

| Visit Type | Helper Method | Service Type Codes |
|------------|---------------|-------------------|
| **Routine Vision** | `IsRoutineVisionVisit()` | Vision: AL, AN, AM, AO, CP |
| `RoutineVision` | ? Contains "ROUTINE" | |
| `AnnualVision` | ? Contains "ANNUAL" | |
| `ComprehensiveExam` | ? Contains "COMPREHENSIVE" | |
| | | |
| **Medical Eye** | `IsMedicalEyeVisit()` | Medical: 30, 98, BR |
| `Medical` | ? Contains "MEDICAL" | |
| `EmergencyEye` | ? Contains "EMERGENCY" | |
| `UrgentEyeCare` | ? Contains "URGENT" | |
| `Infection` | ? Contains "INFECTION" | |
| `Injury` | ? Contains "INJURY" | |
| `Glaucoma` | ? Contains "GLAUCOMA" | |
| `DiabeticEyeExam` | ? Contains "DIABETIC" | |
| `RetinalExam` | ? Contains "RETINAL" | |
| | | |
| **Contact Lenses** | `IsContactLensVisit()` | Vision: AL, AO |
| `ContactLensFitting` | ? Contains "CONTACT" | |
| `CLFitting` | ? Contains "CL" | |
| | | |
| **Glasses/Eyewear** | `IsGlassesOnlyVisit()` | Vision: AM, AO, CP |
| `GlassesDispensing` | ? Contains "GLASSES" | |
| `GlassesDispense` | ? Contains "GLASSES" | |
| `FrameSelection` | ? Contains "FRAME" | |

---

## ? Verification Checklist

- [x] Seed data updated with all missing visit types
- [x] Visit types match helper method string patterns
- [x] LookupApiService extracts Items from LookupSetDto
- [x] LookupCacheService caches visit types in VisitTypes property
- [x] EncounterDetailsSection binds to LookupCache.VisitTypes
- [x] FluentSelect properly configured with OptionText/OptionValue
- [x] Component handles cache loading and CacheLoaded event

---

## ?? Notes

- All visit types use **PascalCase** codes (e.g., `RoutineVision`, not `routine-vision`)
- Helper methods normalize to uppercase and remove spaces for matching
- Seed data uses `SortOrder` to control dropdown display order
- All lookups are **GLOBAL** (TenantId = "GLOBAL") in MVP
- Tenant-specific overrides not yet implemented

---

**Status:** ? COMPLETE  
**Created:** January 2025  
**Author:** Development Team
