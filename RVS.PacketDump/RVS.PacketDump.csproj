<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>RVS.PacketDump.Html</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\RVS.Domain\RVS.Domain.csproj" />
    <!-- PDF renderer lives in RVS.API (carries the QuestPDF dependency). Dev-only tool,
         not deployed, so pulling the API in for a byte[] renderer is acceptable here. -->
    <ProjectReference Include="..\RVS.API\RVS.API.csproj" />
  </ItemGroup>

</Project>
